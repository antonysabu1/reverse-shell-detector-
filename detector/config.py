# Packet size threshold for interactive session detection (bytes)
INTERACTIVE_PKT_SIZE = 128

# Minimum packets to classify as interactive session
MIN_INTERACTIVE_PACKETS = 10

# Entropy threshold for encrypted shell detection (bits per byte)
ENTROPY_THRESHOLD = 5.0

# Alert confidence thresholds
CONFIDENCE_LOW    = 0.30
CONFIDENCE_MEDIUM = 0.60
CONFIDENCE_HIGH   = 0.90
